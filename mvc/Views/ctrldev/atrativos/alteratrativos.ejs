<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Adicionando Atrativos</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-T3c6CoIi6uLrA9TneNEoa7RxnatzjcDSCmG1MXxSR1GAsXEV/Dwwykc2MPK8M2HN" crossorigin="anonymous">
</head>

<body>
    <main>
        <h1>Cadastro de Atrativos</h1>
        <section id="alteratrativos">
            <form action="">
                <div class="row">
                    <div class="col">
                        <label for="nome" class="form-label">Nome do Ponto Túristico</label>
                        <input type="text" value="<%= r.nome %>" name="nome" id="nome" class="form-control"
                            tabindex="1">
                    </div>
                    <div class="col">
                        <label for="lat" class="form-label">Latitude</label>
                        <input type="text" value="<%= r.lat %> name=" lat" id="lat" class="form-control" value=""
                            tabindex="2">
                    </div>
                </div>
                <div class="row">
                    <div class="col">
                        <label for="desc" class="form-label">Descrição</label>
                        <input type="text" value="<%= r.desc %> name=" desc" id="desc" class="form-control"
                            tabindex="4">
                    </div>
                    <div class="col">
                        <label for="long" class="form-label">Longitude</label>
                        <input type="text" value="<%= r.long %> name=" long" id="long" class="form-control" value=""
                            tabindex="3">
                    </div>
                </div>
                <div class="col">
                    <label for="image" class="form-label">Imagem</label>
                    <input type="file" value="<%= r.image %> name=" image" id="image" tabindex="5">
                </div>
                <div class="row">
                    <input type="submit" value="Cadastrar" name="btnattractions" id="enviaratualização"
                        class="btn btn-warning" onclick="enviar()" tabindex="6">
                </div>
            </form>
        </section>
    </main>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-C6RzsynM9kWDrMNeT87bh95OGNyZPhcTNXj1NW7RuBCsyN/o0jlpcV8Qyq46cDfL"
        crossorigin="anonymous"></script>
    <script>
        constform = document.getElementById('enviaratualização')

        form.addEventListener('submit', async (e) => {
            e.preventDefault()

            const nome = document.getElementById("nome").value
            const lat = document.getElementById("lat").value
            const desc = document.getElementById("desc").value
            const long = document.getElementById("long").value
            const image = document.getElementById("image").value
            
            const data = {
                "nome": nome,
                "lat": lat,
                "long": long,
                "desc": desc,
                "image": image
            }

            const dados = await fetch("/atrativos/" + id, {
                method: "PUT"
                
            })
            const json = await dados.json()
            let atrativos = await json
        })
    </script>
</body>

</html>